# Raw material never enters this repo — it is PUBLIC. Mine it in place, commit only findings.
mining/raw/
*.jsonl
*.jsonl.gz
# agent worktrees (Claude Code lane isolation) — never publish
.claude/worktrees/

# secrets and local state
.env
*.env
*.dev.vars
.DS_Store
node_modules/
__pycache__/
scratch/

# demo/fanout run output — regenerated each run, not versioned
demo/out/

# skills pulled from other projects: review before publishing
.claude/skills/
